export declare const code = "/* view-timeline names a timeline tied to the element's viewport intersection.\n   A child element can then subscribe to it by that name. */\n\n.card {\n  view-timeline: --card block;   /* each card publishes its own intersection */\n}\n\n.read-bar {\n  animation: reveal linear both;\n  animation-timeline: --card;    /* consumer reads parent's named timeline */\n  animation-range: entry 0% cover 50%;\n}\n\n@keyframes reveal {\n  from { transform: scaleX(0); }\n  to   { transform: scaleX(1); }\n}";
export default function D6ViewTimeline(): import("react/jsx-runtime").JSX.Element;
